;;; fsm-bit.scm
(define fsm-bit (lambda (str)
  (define fsm-aux (lambda (state bit)
0
 ))

  (fsm-aux (letrec (
  [S0 (lambda (bit) bit)]
  [S1 (lambda (bit) bit)]
  [S2 (lambda (bit) bit)]



  )
  S0
  ) (car str))
))
